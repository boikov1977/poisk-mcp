# ═══════════════════════════════════════════════════════════════
#  MCP SearchTool v3.4 — Dependencies (pinned @ 2026-05-24)
#  Все версии зафиксированы. Не меняй на >= без проверки.
# ═══════════════════════════════════════════════════════════════

# === CORE ===
requests==2.34.2
beautifulsoup4==4.14.3
urllib3==2.7.0

# === ПОИСК ===
ddgs==9.14.4
duckduckgo-search==8.1.1

# === MCP SERVER ===
mcp==1.27.1

# === NLP / NEURAL RERANKING ===
sentence-transformers==5.5.0
numpy==2.4.4

# === PyTorch (CPU-ONLY!) ===
# ВАЖНО: Не добавляй torch сюда — sentence-transformers потянет его сам,
#        но из PyPI он скачает CUDA-версию с гигабайтами nvidia-мусора.
# Устанавливай отдельно через CPU-индекс:
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cpu
# Или через make install-cpu

# === ТРАНЗИТИВНЫЕ (явно для pip-audit) ===
httpx==0.28.1
huggingface-hub==1.15.0
filelock==3.29.0
tokenizers==0.22.2
